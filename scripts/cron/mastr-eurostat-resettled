# Poll Eurostat migr_asyrescra after the catalogue refresh windows
# (11:00 and 23:00 CET). Script no-ops when the cube `updated` stamp is unchanged.
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
MAILTO=""
CRON_TZ=UTC

30 9,10,21,22 * * * root /usr/local/sbin/eurostat-sync-resettled.py 2>&1 | logger -t mastr-eurostat-resettled
